import '../globals.css';

export const metadata = {
  title: 'BetterMonday',
  description: 'Web Design & Development',
}

export default function PostLayout({ children }) {

  return (
    <>
      {children}
    </>
  )
}
